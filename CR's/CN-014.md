# CN-014 - Local PostgreSQL Activation

## Summary

Configured and verified the project against the locally installed PostgreSQL service instead of Docker.

## Changed Files

- `src/db.py`
- `.env.example`
- `README.md`

## Behavior

- Default PostgreSQL DSN now targets the local PostgreSQL service and `capital_kronos` database.
- `db.py` loads `.env` automatically before resolving `POSTGRES_DSN`.
- Every PostgreSQL connection sets the session time zone to UTC.
- The `capital_kronos` database was created locally.
- Migration `001_init` was applied successfully.

## Verification

- PostgreSQL connection succeeded.
- Database tables exist:
  - `market_instruments`
  - `ohlcv_candles`
  - `raw_market_events`
  - `prediction_runs`
  - `forecast_candles`
  - `signals`
  - `prediction_outcomes`
  - `service_heartbeats`
- Dashboard `/api/status` returned `PostgresOk=True`.
- PostgreSQL health now reports timestamps in UTC.

## Safety

No trading execution endpoints or order logic were added.
