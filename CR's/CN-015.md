# CN-015 - Real PostgreSQL End-to-End Test Round

## Summary

Completed a real PostgreSQL-backed test pass after local PostgreSQL became available.

## Tests Run

- Python compile check for all `src` modules.
- PostgreSQL migration idempotency.
- PostgreSQL status summary with masked DSN output.
- Synthetic forecast persistence into PostgreSQL.
- Synthetic actual-candle validation into `WIN` / `LOSS` outcomes.
- Real ETHUSD scheduler run with Capital.com fetch and Kronos inference.
- Validation worker one-shot run.
- Dashboard `/api/status` with PostgreSQL-backed signal/outcome data.
- `start_dashboard.ps1` lifecycle smoke test.
- SQLite residue scan.
- Trading endpoint safety scan.

## Results

- Migrations are applied and idempotent.
- PostgreSQL health is OK and app sessions report UTC timestamps.
- Synthetic test produced 4 outcomes: 2 `WIN`, 2 `LOSS`, 0 `PENDING`.
- Real ETHUSD scheduler run fetched 512 latest candles, ran Kronos, and saved 12 forecast records.
- Latest real ETHUSD signal was stored as an analytical signal.
- Dashboard returned HTTP 200 and reported PostgreSQL OK.
- `start_dashboard.ps1` started dashboard plus background workers and cleanup left 0 project Python processes.
- No SQLite database files remain.
- No trading execution logic was added.

## Notes

The latest real ETHUSD forecast remains `PENDING` until its forecast horizon completes and actual candles are fetched/validated.
